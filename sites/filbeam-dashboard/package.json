{
  "name": "filbeam-dashboard",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "NETWORK=mainnet CLOUDFLARE_DATABASE_ID=$CLOUDFLARE_D1_MAINNET observable build && node scripts/pretty-urls.mjs",
    "clean": "rimraf src/.observablehq/cache",
    "dev": "NETWORK=mainnet CLOUDFLARE_DATABASE_ID=$CLOUDFLARE_D1_MAINNET observable preview --host 0.0.0.0"
  },
  "dependencies": {
    "@observablehq/framework": "^1.12.0",
    "assert-ok-response": "^1.0.0",
    "d3-dsv": "^3.0.1",
    "d3-time-format": "^4.1.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=22"
  }
}
